<h1>Importar Produtos</h1>

<%= form_with model: @upload, local: true do |f| %>
  <div class="field">
    <%= f.label :file, "Arquivo CSV" %>
    <%= f.file_field :file %>
  </div>

  <div class="actions">
    <%= f.submit "Importar" %>
  </div>
<% end %>
